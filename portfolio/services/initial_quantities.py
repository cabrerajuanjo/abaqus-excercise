

from portfolio.models.models import Asset, Portfolio, Price, Weight


def calculate(initial_values: dict[str, int], portfolios: list[Portfolio], assets: list[Asset], weights: list[Weight], prices: list[Price]) -> dict[str, int]:
    return initial_values
